# Build outputs
*.exe
*.obj
*.pdb
obj/
imgui.ini

# Local screenshots (but keep committed brand assets)
*.png
!assets/*.png

# Scan results (contain local disk contents)
scan*.csv
scan*.json
scan_output.txt
results.csv
results.json

# Snapshots
*.ysnap

# Update-signing PRIVATE keys - must never be committed. Only the public key
# (src/update_pubkey.h) belongs in the repo. See tools/gen-signing-key.ps1.
.keys/
*.pem
yadua-signing-key*
